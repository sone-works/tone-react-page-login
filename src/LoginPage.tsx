'use client'

import { Box, Page } from '@sone-dao/tone-react-core-ui'

interface ILoginPageProps {}

export default function LoginPage({}: ILoginPageProps) {
  return (
    <Page>
      <Box></Box>
    </Page>
  )
}
